<mvc:View
    controllerName="restaurant.finalproject.controller.ProductList"
	xmlns:f="sap.f"
    xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
    xmlns:tnt="sap.tnt"
    displayBlock="true"
    xmlns="sap.m"
>
		<App>
            <pages>
				<Page id="page" title="{i18n>title}">
					<tnt:ToolHeader>
						<SearchField placeholder="{i18n>productNameFilter}"
									 width="40%"
									 search="onFilterProductName"/>
						<MultiComboBox id="productsMCB"
									width="40%"
									selectionChange="onFilterProductType">
										<core:ListItem key="" text=""/>
						</MultiComboBox>
						<Button text="{i18n>addProductButton}" press="handlePopoverAddProducts"/>
					</tnt:ToolHeader>
					<Table id="idProductsTable"
						   items="{products>/value}"
						   class="sapUiResponsiveContentPadding">
						<columns>
							<Column>
								<Text text="{i18n>productName}"/>
							</Column>
							<Column>
								<Text text="{i18n>productQuantity}"/>
							</Column>
							<Column>
								<Text text="{i18n>productBranch}"/>
							</Column>
							<Column>
								<Text text="{i18n>productPrice}"/>
							</Column>
							<Column>
								<Text text="{i18n>productCaducity}"/>
							</Column>
                            <Column>
								<Text text="{i18n>productType}"/>
							</Column>
						</columns>
						<items>
							<ColumnListItem>
								<cells>
									<Text text="{products>name}"/>
									<Text text="{products>quantity}"/>
									<Text text="{products>branch}"/>
									<Text text="{products>price-kg}"/>
									<Text text="{path: 'products>caducity', 
										formatter: 'restaurant.finalproject.util.Formatter.setDate'}"/>
                                    <Text text="{products>type}"/>
								</cells>
							</ColumnListItem>
						</items>
					</Table>
					<footer >
        				<Bar>
          					<contentLeft>
            					<Text text="{i18n>footerTitle}"></Text>
          					</contentLeft>
          					<contentRight>
            					<Text text="{i18n>footerProducts}"></Text>
          					</contentRight>
        				</Bar>
      				</footer>
				</Page>
			</pages>
        </App>
</mvc:View>